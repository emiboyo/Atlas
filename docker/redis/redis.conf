bind 0.0.0.0
# Access is restricted by Compose's internal backend network and loopback-only
# host publishing. Protected mode rejects legitimate peer-container traffic
# when Redis is intentionally configured without local-development credentials.
protected-mode no
port 6379
appendonly yes
appendfsync everysec
save 60 1000
maxmemory-policy noeviction
